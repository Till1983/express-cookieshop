<!DOCTYPE html>
<html lang="en">
    <%- include('../includes/head', {title: 'Shop | Cookieshop'}) %>
<body>
    <%- include('../includes/header') %>

    <main>
      <p>Our current offering:</p>
      <ul>
        <% cookies.forEach(cookie => { %>
          <li>
            <% if(cookie.isInStock) { %> 
              <a href="/cookies/<%= cookie.slug %>"><%= cookie.name %></a>: <%= readablePrice(cookie.priceInCents) %>
            <% } else { %>
              <%= cookie.name %> [SOLD OUT] 
            <% } %>
          </li>
        <% }) %>
      </ul>
      <p>Get involved! Contribute to our stock <a href="/cookies/new">here</a></p>
    </main>

  <%- include('../includes/footer') %>
</body>
</html>